services:
  server:
    image: phone-back-dev
    build:
      context: .
      dockerfile: dev.Dockerfile
    volumes:
      - ./:/usr/src/app
    ports:
      - 3005:3005
    container_name: todo-back-dev
    # environment:
    #   env_file: '.env'
